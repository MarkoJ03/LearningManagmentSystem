import { OnHoverDisplayDirective } from './on-hover-display.directive';

describe('OnHoverDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new OnHoverDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
